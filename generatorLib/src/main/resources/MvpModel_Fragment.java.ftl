package ${packageImport};

import ${basePackage}.base.mvp.BaseModel;
import androidx.fragment.app.Fragment;

/**
 * @author liujiang
 * created at: ${.now}
 * Desc: ${desc}
 */
public class ${modelName} extends BaseModel {

    public ${modelName}(Fragment fragment) {
        super(fragment);
    }

}