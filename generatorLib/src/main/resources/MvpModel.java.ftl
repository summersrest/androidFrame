package ${packageImport};

import ${basePackage}.base.mvp.BaseModel;
import android.content.Context;

/**
 * @author liujiang
 * created at: ${.now}
 * Desc: ${desc}
 */
public class ${modelName} extends BaseModel {

    public ${modelName}(Context context) {
        super(context);
    }

}